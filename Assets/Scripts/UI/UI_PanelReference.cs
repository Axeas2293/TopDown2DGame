using UnityEngine;

public class UI_PanelReference : MonoBehaviour
{
    public GameObject UI_Panel_Menu;    
    public GameObject UI_Panel_Inventory_Bar;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
